# UniCardioRF 模型结构配置
#
# channels 必须等于 len(kernel_sizes) * per_kernel_channels；

slot_length: ${data.slot_length}
channels: 288          # 6 个卷积核 * 每个核 48 通道
nheads: 9
n_layers: 5
time_embedding_dim: 256
kernel_sizes: [1, 3, 5, 7, 9, 11]
per_kernel_channels: 48
ffn_dim: 64
